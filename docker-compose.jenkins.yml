services:
  jenkins:
    build:
      context: ./jenkins
      dockerfile: Dockerfile
    container_name: blog-jenkins
    ports:
      - "8081:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - ./:/workspace/blog:ro
    restart: unless-stopped

volumes:
  jenkins_home:
